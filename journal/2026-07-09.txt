Session Summary — July 9, 2026
Started: 11:00 PM
=================================

- Designed the Opening model for the database
  - Fields: name, eco_code, first_move_white, first_move_black
  - Each Game gets a foreign key to Opening (nullable, SET_NULL)
  - Discussed whether games need one or two openings — decided one is correct
  - Discussed boolean fields vs CharField for first moves — settled on storing
    the actual move text (e.g. "e4", "c5") in separate white/black fields

- Wrote all code changes needed to add the Opening model
  - Opening model in models.py (placed above Game)
  - OpeningSerializer in serializers.py
  - OpeningViewSet in views.py
  - Registered /api/openings/ route in urls.py
  - Migration commands

- Added full "Opening Model" section to DEVLOG.md with step-by-step instructions

- Researching ECO tables for populating the Opening model

- Added the Report model to the database
  - Fields: name, created_at (auto), user (ForeignKey to User), games (ManyToManyField)
  - CASCADE on user deletion, blank=True on games so reports can exist before games are added
  - Django auto-creates junction table for the ManyToMany relationship
  - Added ReportSerializer, ReportViewSet, and registered /api/reports/ route
  - Wrote full "Report Model" section in DEVLOG.md with step-by-step instructions

- Decided against an Opening model — ECO data is a fixed lookup table, not user data
  - No model, serializer, or migration needed
  - Downloaded JSON files locally instead of querying a database table

- Found ECO data source: JeffML/eco.json repo on GitHub
  - https://github.com/JeffML/eco.json
  - Data split across 5 files: ecoA.json through ecoE.json
  - Each file is a dictionary keyed by FEN position
  - Values contain eco code, opening name, and move sequence
  - Explored the data from the Django shell using requests library

- Saved ECO JSON files locally in backend/eco/
  - Parser will load them at runtime, no network dependency

- Decided to match openings by FEN position rather than move text
  - FEN matching handles transpositions (different move orders, same position)
  - python-chess already provides FEN after each move via board.fen()

- Installed ipython for a better Django shell experience

- Added ECO data section to DEVLOG.md

- Next: update pgn_parser.py to create a Report per upload and link games to it
