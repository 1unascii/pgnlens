Session Summary — June 27-28, 2026
====================================

TUNEOPEDIA
----------
- Added MIDI player, instrument dropdown, and tablature controls
  to the setting show view (abc-renderer.js + setting.blade.php)
- Instrument changes rebuild the synth controller and re-render
  with the new MIDI program
- Seeded 86 tunes from nhcountrydance.abc, cocks-northumberland-1.abc,
  and cocks-northumberland-2.abc
- Three collections: NH Country Dance (22), Cocks 1 (30), Cocks 2 (34)
- Committed and pushed

PORTFOLIO RESUME
----------------
- Changed email to josephearlweiner83@gmail.com
- Section headings simplified: Skills, Projects, Experience, Education
- Removed "Other" skill category
- Skills, project highlights, and job responsibilities all shuffle
  randomly on each page load
- Added editable project descriptions for admin
- Added certificate location column (Madison, WI), removed state column
- Added drag-and-drop project reordering (auth only)
- Fixed mixed content by setting APP_URL and adding TrustProxies

PORTFOLIO — INTERVIEWS PAGE
----------------------------
- New auth-only page at /interviews
- Rich text editor with toolbar (bold, italic, headings, lists)
- Content saves to storage/app/interviews/content.json
- Ctrl+S shortcut to save

TRIP PLANNER
------------
- Split Google API keys: client (referrer-restricted) and server (hidden)
- Moved Directions API to server-side PHP proxy (api/directions.php)
- api/config.php holds server key, gitignored
- Optimize Order button greyed out when fewer than 4 stops
- Committed and pushed

ABC NOTATION EDITOR
-------------------
- Fixed accidental playback to capture all octave modifiers
- Removed dead code and debug logs
- Renamed folder to abc-notation-editor
- Committed and pushed test suite

PGNLENS
-------
- Set up Django project with pipenv
- Installed Waitress for production serving on Windows
- Configured Apache reverse proxy: pgnlens.com -> Waitress on port 8002
- Static files served directly by Apache
- Basic home page running at pgnlens.com
- Explored wireframe.make files, extracted thumbnails
- Wireframe shows Analysis Reports page with game stats,
  opening performance chart, lines that need practice, and
  opening repertoire breakdown

DJANGO + REACT TUTORIAL (django-todo-react)
-------------------------------------------
- Built full Todo app: Django REST backend + React frontend
- Used Vite instead of create-react-app (faster setup)
- Vite differences from CRA:
    - main.jsx instead of index.js
    - .jsx extension required for JSX files
    - proxy configured in vite.config.js not package.json
    - npm run dev instead of npm start
    - runs on port 5173 instead of 3000
- Configured CORS and CSRF for Vite dev server (port 5173)
- Installed JWT auth (djangorestframework-simplejwt)
- Added token endpoints and AuthContext
- Tutorial moved on to next step before login form and registration

