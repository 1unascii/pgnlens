Session Summary — July 18, 2026
=================================
Estimated hours: 5

- Updated DEVLOG_FRONTEND.md throughout the session
  - Added note about replacing the Vite boilerplate in App.tsx
  - Added where the Report interface goes (src/types.ts with export)
  - Corrected fetch example to live in ReportIndex, not App.tsx
  - Added import type syntax for Report imports
  - Added ReportCreate page and /reports/create route
  - Moved upload form out of ReportIndex into its own ReportCreate page
  - Added game component placeholders (GameIndex, GameCreate, GameView)
  - Added React Router and import type references

- Fixed bugs and created BUGFIX.md to track them
  - vite.config.ts had stray "op" text at end of file
  - App.tsx missing closing brace for App function
  - Report import needed import type (verbatimModuleSyntax)
  - Route path mismatch — component mapped to "/" but visiting "/reports"

- Turned off semicolon enforcement in ESLint config
  - Added semi: ['error', 'never'] rule in eslint.config.js

- Renamed uploadPgn to uploadPGNFileAndCreateReport
  - PGN should stay all-caps since it's an acronym

- Restructured page routing
  - /reports — ReportIndex lists reports with "Upload PGN File" link
  - /reports/create — ReportCreate has the upload form
  - /reports/:id — ReportView for a single report
  - /games — GameIndex for finding/joining games

- Discussed naming conventions
  - Routes use plural nouns (REST convention): /reports, /games
  - Component names use singular (programming convention): ReportIndex, GameView
  - Decided to keep component names plural for now

- Chose frontend libraries
  - react-chessboard + chess.js for board display and chess logic
  - Tailwind CSS for styling (most in-demand for jobs)
  - react-icons for icons (includes Font Awesome chess piece icons)

- Built GameView component to load and replay saved games
  - Fetches game data from GET /api/games/:id/
  - Fetches moves from GET /api/moves/?game=:id
  - Replays moves with chess.js to build array of FEN positions
  - Displays board with react-chessboard (v5 uses options prop)
  - Navigation buttons: Next, Previous, Start, End

- Fixed MoveViewSet — had no game filtering
  - GET /api/moves/?game=600 was returning ALL moves from every game
  - Added get_queryset override to filter by game query param

- Fixed PGN parser duplicate move bug
  - pgn_parser.py line 81: "if white_move is not None" always true (it's a string)
  - Should check "if board.turn == chess.BLACK" instead
  - Added try/catch in GameView as a workaround for bad data

- Fixed react-chessboard v5 API
  - v5 uses options prop: <Chessboard options={{ position: fen }} />
  - Not individual props like older versions

- Added Lichess move sounds
  - Downloaded Move.mp3 and Capture.mp3 from lichess-org/lila
  - Stored in frontend/public/sound/lichess/standard/
  - playMoveSound() uses new Audio() to play on button click

- Downloaded all 42 Lichess piece sets to frontend/public/piece/
  - 39 SVG sets work out of the box
  - mono — same piece for both colors, had to copy files to w/b prefixed names
  - monarchy — uses .webp not .svg, added extension parameter to makePieceSet
  - disguised — all pieces look the same (that's the point)
  - Created makePieceSet() helper to swap themes with one string change

- Board customization
  - darkSquareStyle / lightSquareStyle to change board colors
  - Tailwind [image-rendering:pixelated] class for retro pixel look
  - Can combine pixel piece set with greyscale squares

- More bugs fixed and added to BUGFIX.md
  - main.tsx importing deleted index.css
  - Wrong moves API URL (/api/games/600/moves vs /api/moves/?game=600)
  - chess.js rejects raw UCI strings — need {from, to, promotion} object
  - Extra closing brace in chess.move() call
  - Iterating over property names instead of values in move loop
