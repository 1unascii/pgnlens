2026-08-07

VITEST SETUP

Installed vitest, testing-library, jest-dom,
user-event, and jsdom. Configured vite.config.ts,
tsconfig.app.json, setup file, and package.json
test scripts.

TEST PLAN

Wrote test plan in DEVLOG_TESTING.md. Priority 1
is pure functions (generateChartColors, handleSortChange,
filterSortPaginate). Priority 2 is components with
logic (SortButtons, PaginationControls, OpeningLineCard).
Skipping GameView and heavy fetch components for now.

EXTRACT filterSortPaginate

Extracted the repeated filter/sort/slice chain from
ReportView into a reusable utility function. Wrote
and ran 13 tests for it, all passing.

Wrote full test code in the devlog for SortButtons,
PaginationControls, and OpeningLineCard.

BACKEND TESTS

Ran existing pytest suite. Found 8 failures from
outdated tests — deleted Move model reference, fixed
Report field name, updated 6 parser tests to match
current family classifications.

EMAIL VERIFICATION

Switched from console email backend to Resend SMTP.
django-resend package doesn't support Python 3.14,
so using Django's built-in SMTP backend with Resend's
SMTP gateway instead. No extra packages needed.

Had to pin Django back to 6.0.7 after pipenv upgraded
it to 6.1, which broke DRF (cc_delim_re removed).

Emails send intermittently. Added debugging and
custom HTTP API backend instructions to the devlog
as a more reliable alternative to SMTP.

USER AUTHENTICATION

Set up login with username (was trying email before).
Added AUTHENTICATION_BACKENDS for allauth. Login
method now supports both username and email.

Created authHeaders utility function to centralize
token and CSRF header construction. Updated
ReportIndex, ReportView, and ReportCreate to use it.

Scoped reports to authenticated user with
get_queryset override in ReportViewSet.

Added logged-in/logged-out navbar state — shows
Logout and Profile when authenticated, Login and
Register when not.

Created basic Profile page showing username and email.

DEVLOG ORGANIZATION

Moved all devlogs into devlogs/ folder. Created
DEVLOG_USER_AUTHENTICATION.md and
DEVLOG_DEPLOYMENT.md. Reformatted heading style
across devlogs.
