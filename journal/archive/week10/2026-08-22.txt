2026-08-22

PRODUCTION SERVER SYNC

Downloaded settings.py and urls.py from production
server via scp. Made settings.py environment-aware:
DEBUG and ALLOWED_HOSTS now read from .env with
defaults for local dev. vite.config.ts base path
already conditional on build mode.

EMAIL HANDLER

Created email_handler.py with ResendHTTPBackend
using Resend's HTTP API instead of SMTP.
DigitalOcean blocks outbound SMTP ports, so the
custom backend sends email via Resend's Python SDK.
Fixed settings.py to point to the correct module
path (game_analyzer.email_handler).

BACKUPS

Set up automated database backups on the server:
- Created backup.sh script for PostgreSQL and MySQL
  dumps to /home/earl/backups/
- Fixed pg_dump to use -h localhost for password
  auth instead of peer auth
- Created .pgpass for unattended PostgreSQL backups
- Granted PROCESS privilege to MySQL tuneopedia_user
- Set up cron job to run backups daily at 3 AM UTC

DEVLOG AND DOCUMENTATION

Created MARKETING.md in devlogs/ covering
monetization strategy, credit bundle system,
financial projections, and lichess asset licensing.
Monarchy piece set (currently in use) has no
explicit license from creator slither77.

TESTING AND TOOLING

Created run-tests.sh in project root to run both
backend (pytest) and frontend (vitest) test suites.
Created start-server.sh scripts in backend/ and
frontend/ directories. Added bash aliases
(start-backend, start-frontend) for convenience.
Ran collectstatic locally to eliminate whitenoise
warning. All 72 tests passing (35 backend,
37 frontend), 92% backend coverage.
