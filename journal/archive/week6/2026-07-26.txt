2026-07-26

- Added "Lines to Practice" section to ReportView — cards showing opening lines
  under a win rate threshold with W/L/D record and total games
- Refactored sortBy state from a simple string to an object with chart and weakLines
  keys so each section sorts independently
- Refactored pagination state to currentPage object with weakLines and allOpenings keys
- Renamed openingData to familyChartData for clarity
- Added minimum games filter (number input) — applies to both chart and cards
- Added win rate threshold slider — controls which lines show in "Lines to Practice"
- Both filters reset pagination to page 0 when changed to avoid blank pages
- Extracted tooltip formatter into a named function with a ToolTipContents type
- Pagination bug: Next button allows going past the last page — still investigating
- Added "All Openings" section with paginated family cards sorted by win rate
- Added conditional red/green color on win rate (red under 50%, green at or above)
- Fixed pagination filters — all openings section only filters by minimumGames,
  not winRateThreshold (that's only for weak lines)
- Fixed all openings section using wrong page key (was weakLines, now allOpenings)
- Extracted StatCard component — displays a label and bold value, used for summary
  stats (total games, win rate, openings count) at top of report
- Documented StatCard in DEVLOG_FRONTEND.md
- Removed unused xAxisHeight calculation that was left after extracting OpeningBarChart
  — also fixed a crash since it accessed report data before the null check
- Removed unused formatToolTip function and ToolTipContents type (now in OpeningBarChart)
- Removed unused recharts import (now in OpeningBarChart)
- Documented all three cleanup fixes in BUGFIX.md
- Replaced hardcoded CHART_COLORS array (had duplicates) with generateChartColors()
  — evenly spaces hues around the HSL color wheel so all colors are unique
