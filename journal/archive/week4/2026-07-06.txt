Session Summary — July 6, 2026
Duration: ~2 hours
=================================

TUNEOPEDIA — abcjs fork investigation
--------------------------------------

- Investigated how fretted drone string works in tuneopedia
  - UI checkbox appends _fretted to instrument name
  - Hardcoded banjo _fretted variants omit maxFrets to allow drone fretting
  - customBanjo uses isBanjo flag for dynamic maxFrets generation
  - frettedDrone flag overrides dynamic maxFrets for customBanjo_fretted
  - Noted inconsistency: hardcoded _fretted variants don't use frettedDrone flag

- Fixed custom banjo tablature not loading with empty tuning
  - UI code was bailing out before abcjs could use defaultTuning
  - Fixed guard in abc-renderer.js and tune_form.js

- Fixed missing closing > on input tag in tune-form.blade.php

- Added live rerendering on tuning input (changed @change to @input)
  - Added 500ms debounce to prevent rapid render crashes

- Removed guard that killed tablature when fewer than 2 notes typed
  - Now falls back to default tuning instead of removing tablature

- Discovered infinite loop bug in abcjs fork (tab-notes.js)
  - tabNotes() crashes when two adjacent strings have the same pitch
  - while loop increments past toN without ever matching sameNoteAs
  - Affects all instruments, not just custom banjo
  - Fix identified but deferred — requires editing the fork source

- Noted frettedDrone parameter may be unnecessary
  - Could be replaced by just omitting maxFrets like hardcoded variants
  - Deferred refactor for future session
